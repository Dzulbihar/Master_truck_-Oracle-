CREATE TABLE DASHBOARD.MS_BENTUK_PELANGGARAN
(
  BENTUK_PELANGGARAN  VARCHAR2(255 BYTE)        NOT NULL,
  BENTUK_JENIS        VARCHAR2(255 BYTE)        NOT NULL,
  STATUS              VARCHAR2(255 BYTE)        DEFAULT 'Aktif'               NOT NULL,
  CREATED_AT          TIMESTAMP(6),
  UPDATED_AT          TIMESTAMP(6)
)