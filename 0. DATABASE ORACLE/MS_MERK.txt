CREATE TABLE DASHBOARD.MS_MERK
(
  ID          NUMBER(19)                        NOT NULL,
  KODE_ITEM   VARCHAR2(255 BYTE),
  KETERANGAN  VARCHAR2(255 BYTE),
  STATUS      VARCHAR2(255 BYTE)                DEFAULT 'Aktif'               NOT NULL,
  CREATED_AT  TIMESTAMP(6),
  UPDATED_AT  TIMESTAMP(6)
)